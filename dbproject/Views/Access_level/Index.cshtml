@model IEnumerable<dbproject.Access_level>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.access_value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.access_value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.access_code }) |
            @Html.ActionLink("Details", "Details", new { id=item.access_code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.access_code })
        </td>
    </tr>
}

</table>
